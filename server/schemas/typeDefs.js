const { gql } = require('apollo-server-express');

// definte our types here. Schema for objects that should match mongo schema
// queries and mutations to match our resolvers
const typeDefs = gql`
`;

module.exports = typeDefs;
